{
  "name": "user-access",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/user-access/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"],
        "cwd": "apps/user-access"
      },
      "configurations": {
        "development": {
          "args": ["--node-env=development"]
        }
      }
    },
    "prune-lockfile": {
      "dependsOn": ["build"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/dist/apps/user-access/package.json",
        "{workspaceRoot}/dist/apps/user-access/pnpm-lock.yaml"
      ],
      "options": {
        "buildTarget": "build"
      }
    },
    "copy-workspace-modules": {
      "dependsOn": ["build"],
      "cache": true,
      "outputs": ["{workspaceRoot}/dist/apps/user-access/workspace_modules"],
      "executor": "@nx/js:copy-workspace-modules",
      "options": {
        "buildTarget": "build"
      }
    },
    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "user-access:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "user-access:build:development"
        },
        "production": {
          "buildTarget": "user-access:build:production"
        }
      }
    },
    "test": {
      "options": {
        "passWithNoTests": true
      }
    },
    "build-seed": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build --config webpack.seed.config.js",
        "args": ["--node-env=production"],
        "cwd": "apps/user-access"
      }
    },
    "seed": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-seed"],
      "options": {
        "command": "node ../../dist/apps/user-access-seed/main.js",
        "cwd": "apps/user-access",
        "forwardAllArgs": true
      }
    }
  }
}
